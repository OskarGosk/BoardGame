package com.goskar.boardgame.ui.theme

import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.sp
import com.goskar.boardgame.R

val Smooch14 = TextStyle(
    fontSize = 14.sp,
    fontFamily = FontFamily(Font(R.font.smooch_sans_medium)),
    fontWeight = FontWeight(400)
)

val Smooch14_line14 = TextStyle(
    fontSize = 14.sp,
    fontFamily = FontFamily(Font(R.font.smooch_sans_medium)),
    fontWeight = FontWeight(400),
    lineHeight = 12.sp
)

val Smooch16 = TextStyle(
    fontSize = 16.sp,
    fontFamily = FontFamily(Font(R.font.smooch_sans_medium)),
    fontWeight = FontWeight(400)
)

val Smooch18 = TextStyle(
    fontSize = 18.sp,
    fontFamily = FontFamily(Font(R.font.smooch_sans_medium)),
    fontWeight = FontWeight(400)
)

val Smooch20 = TextStyle(
    fontSize = 20.sp,
    fontFamily = FontFamily(Font(R.font.smooch_sans_medium)),
    fontWeight = FontWeight(400)
)
val Smooch22 = TextStyle(
    fontSize = 22.sp,
    fontFamily = FontFamily(Font(R.font.smooch_sans_medium)),
    fontWeight = FontWeight(400)
)



//BOLD

val SmoochBold16 = TextStyle(
    fontSize = 16.sp,
    fontFamily = FontFamily(Font(R.font.smooch_sans_bold)),
    fontWeight = FontWeight(400)
)

val SmoochBold18 = TextStyle(
    fontSize = 18.sp,
    fontFamily = FontFamily(Font(R.font.smooch_sans_bold)),
    fontWeight = FontWeight(400)
)

val SmoochBold20 = TextStyle(
    fontSize = 20.sp,
    fontFamily = FontFamily(Font(R.font.smooch_sans_bold)),
    fontWeight = FontWeight(400)
)

val SmoochBold22 = TextStyle(
    fontSize = 22.sp,
    fontFamily = FontFamily(Font(R.font.smooch_sans_bold)),
    fontWeight = FontWeight(400),
    letterSpacing = 2.sp
)

val SmoochBold24LetterSpacing2 = TextStyle(
    fontSize = 24.sp,
    fontFamily = FontFamily(Font(R.font.smooch_sans_bold)),
    fontWeight = FontWeight(400),
    letterSpacing = 2.sp
)

val SmoochBold26 = TextStyle(
    fontSize = 26.sp,
    fontFamily = FontFamily(Font(R.font.smooch_sans_bold)),
    fontWeight = FontWeight(400)
)


//EXTRA BOLD

val SmoochExtraBold16 = TextStyle(
    fontSize = 16.sp,
    fontFamily = FontFamily(Font(R.font.smooch_sans_extra_bold)),
    fontWeight = FontWeight(400)
)

val SmoochExtraBold20 = TextStyle(
    fontSize = 20.sp,
    fontFamily = FontFamily(Font(R.font.smooch_sans_extra_bold)),
    fontWeight = FontWeight(400)
)

val SmoochExtraBold24 = TextStyle(
    fontSize = 24.sp,
    fontFamily = FontFamily(Font(R.font.smooch_sans_extra_bold)),
    fontWeight = FontWeight(400),
    letterSpacing = 2.sp
)

val SmoochExtraBold32 = TextStyle(
    fontSize = 32.sp,
    fontFamily = FontFamily(Font(R.font.smooch_sans_extra_bold)),
    fontWeight = FontWeight(400)
)